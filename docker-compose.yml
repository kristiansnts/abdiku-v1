services:
  app:
    build: .
    ports:
      - "9000:8000"
    env_file: .env
    environment:
      - APP_ENV=production
      - APP_DEBUG=false
    volumes:
      - storage:/app/storage
    restart: unless-stopped

volumes:
  storage:
