services:
  app:
    build: .
    ports:
      - "8082:8080"
    env_file: .env
    environment:
      - APP_ENV=production
      - APP_DEBUG=false
    volumes:
      - storage:/app/storage
    restart: unless-stopped

volumes:
  storage:
